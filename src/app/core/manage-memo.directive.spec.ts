import { ManageMemoDirective } from './manage-memo.directive';

describe('ManageMemoDirective', () => {
  it('should create an instance', () => {
    const directive = new ManageMemoDirective();
    expect(directive).toBeTruthy();
  });
});
